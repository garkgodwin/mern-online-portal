import React from "react";
import { Container, Typography } from "@mui/material";
const Subjects = () => {
  return (
    <Container sx={{ mt: 8, py: 8, width: 1 }}>
      <Typography variant="h4">Registrar Subjects Page</Typography>
      <Typography variant="h5">spr</Typography>
      <Typography variant="h5">spr per course</Typography>
      <Typography variant="h5">can filter</Typography>
    </Container>
  );
};

export default Subjects;
