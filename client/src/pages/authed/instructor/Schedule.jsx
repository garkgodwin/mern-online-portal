import React from "react";
import { Container, Typography } from "@mui/material";

const Schedule = () => {
  return (
    <Container sx={{ mt: 8, py: 8, width: 1 }}>
      <Typography variant="h4">
        Instructor Schedule Page : WITH CALENDAR
      </Typography>
    </Container>
  );
};

export default Schedule;
