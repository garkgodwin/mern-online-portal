import React from "react";
import { Container, Typography } from "@mui/material";

const Employees = () => {
  return (
    <Container sx={{ mt: 8, py: 8, width: 1 }}>
      <Typography variant="h4">Administrator Employees Page</Typography>
    </Container>
  );
};

export default Employees;
